use.miden_name::naming
use.miden::note
use.std::sys

const.INITIALIZE_NOTE_INPUT_PTR=0

# Input (arguments): [token_prefix, token_suffix, contract_prefix, contract_suffix]
begin
    push.INITIALIZE_NOTE_INPUT_PTR
    exec.note::get_inputs
    # [num_inputs, init_ptr]
    drop drop push.INITIALIZE_NOTE_INPUT_PTR
    mem_loadw
    # [INPUTS]
    call.naming::set_payment_token_contract
    exec.sys::truncate_stack
end