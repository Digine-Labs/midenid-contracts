use.miden_name::naming
use.miden::note
use.std::sys

const.PRICING_CONTRACT_PTR=0
const.ROOT_PTR=4
# Input (arguments): [PRICING_CONTRACT, ROOT]
begin
    push.0
    exec.note::get_inputs
    drop drop
    mem_loadw.ROOT_PTR padw mem_loadw.PRICING_CONTRACT_PTR
    # [prefix, suffix, DOMAIN]
    call.naming::set_calculate_price_root
    exec.sys::truncate_stack
end