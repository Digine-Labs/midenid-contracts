use.miden_id::registry
use.std::sys

# Input: [NAME]
begin
    call.registry::get_id_by_name
    # [id_prefix, id_suffix]
    exec.sys::truncate_stack
end

# TODO
# This script for resolving name utilities